
	
	<article>
		
        <div class="container">
            <div class="row">
                <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                	<% @posts.each do |post| %>   	
             		<h2><%= post.title %></h2></br> 
					<hr>
					<p><%= post.body%></p>
					<p>By:<%= post.author%></p>
					<hr>
                </div>
            </div>
        </div>
        <% end %>
    </article>